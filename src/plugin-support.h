#pragma once
// Minimal plugin-support header. In the upstream obs-plugintemplate this file is
// generated from plugin-support.c.in; we provide a static version so the project
// builds standalone. Keep PLUGIN_VERSION in sync with CMakeLists / buildspec.json.
#ifndef PLUGIN_VERSION
#define PLUGIN_VERSION "1.0.0"
#endif
